# Impact Leaders API Configuration (Client-side - uses proxy)
NEXT_PUBLIC_API_BASE_URL=/api/proxy

# Backend API URL (Server-side only - for proxy)
API_BASE_URL=http://65.1.100.129:5500

# Server Configuration (Optional)
# PORT=3000
